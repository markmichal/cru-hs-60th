# Handoff — Cru HS 60th Anniversary Site

> **WHERE WE ARE (2026-06-25).** Front-end batch + identity shift landed in `index.html`, plus a one-time data-cleanup utility in `apps-script-reference.gs`. The site is shifting from a one-time 60th-anniversary site to a permanent **"Cru High School Legacy"** living archive (renamed from "Cru High School History" on 2026-06-27 — see that section below). A later same-day pass added three more pure-front-end tweaks (share.html scroll fix, filter-bar layout, bigger logo) that need **no redeploy** — see "2026-06-25 (later) — three front-end tweaks". See "2026-06-25 — front-end batch + identity shift" for the identity work. **Owner actions remaining (from the identity batch — the later tweaks add none):**
> 1. ⚠️ **SUPERSEDED 2026-06-28 — do NOT run `convertStoryTypeToPhoto()`.** The plan reversed: "Story" is a real type again, so Story-typed rows must STAY "Story" (see "2026-06-28 — type system cleanup" below). You still paste the latest `apps-script-reference.gs` whenever you do your next redeploy, but **skip the Convert Story → Photo step entirely.** The menu item is now obsolete; ignore it. ~~Paste the updated `apps-script-reference.gs` … then run `convertStoryTypeToPhoto()` ONCE … rewrites any `Type = "Story"` rows on the Stories tab to `Photo`.~~
> 2. **Redeploy a NEW web-app version** only if the **Stage 2 auto-rename** (the 2026-06-24 change) isn't live yet — pasting the file above also carries it. If you already redeployed Stage 2, no redeploy is needed for this batch.
> 3. **Eyeball the live site** after the Pages rebuild (~1–2 min): the logo sits cleanly in the yellow badge, the header reads "Cru High School Legacy" (see the 2026-06-27 rename), the nav reads Timeline · Stories · People · Locations, and the gallery's decade buttons filter correctly alongside type/search/location/person.
>
> **Note on editable text:** the header title, subtitle, and nav labels are still editable via the hidden editor → Site Settings tab. The HTML defaults were updated, but **if a saved Site Settings value exists for `headerTitle` / `headerSubtitle` / `navGallery`, that saved value wins** and the old text may still show — clear/update it in the hidden editor (PIN 6060) if so.
>
> **Latest (2026-07-02): second machine fully online; "DOCTYPE" scare traced to a stale browser cache; Priority 1 (Clasp) DONE.** Mark's new MacBook is now a fully working second machine: git identity set, GitHub push access configured (personal access token in the Mac's keychain), Node.js + `clasp` (Google's Apps Script CLI) installed and logged in. Mark hit the classic `Unexpected token '<', "<!DOCTYPE"... is not valid JSON` error while saving service history on `share.html`; a live probe of the Apps Script's POST path looked broken (405 + HTML) and pointed at a server-side redeploy problem, but the actual fix was **a hard reset of Mark's browser on the new machine** (stale cached page/response) — the live site and Apps Script were never actually down. **Lesson for next time:** this error can be a stale-browser false alarm, not just a redeploy issue — check "does a hard refresh fix it" before assuming the server is broken. Also added two standing sections to `CLAUDE.md`: **"Session Rhythm"** (auto `git pull` + read HANDOFF at session start; auto update-HANDOFF-and-push at session end) and a **"Working with Mark"** note (he has no coding background — explain plainly, plan before building). **Priority 1 shipped:** using `clasp`, discovered the live Apps Script actually has 9 files, not the 1 (`Code.js`/`apps-script-reference.gs`) the repo tracked — the other 7 (`Dropdowns.js`, `corrections.js`, `Reformat.js`, `checkboxes.js`, `newform.js`, `newform2.js`, `newform4.js`) existed ONLY inside Google with no backup anywhere. All 9 are now mirrored in a new `apps-script/` folder in the repo (see its README), confirmed `Code.js` matches `apps-script-reference.gs` exactly (trailing-newline only diff). Identified the live deployment ID (`AKfycbwNrxCjE2WFOLNxkwVzNblEeRN2q7qyVbz_AqAexL1ho5U0GtPX6meS62fhcCN5wu0y`, was `@19`) and proved a **one-command deploy** works end-to-end with a no-op test push+deploy to `@20` — confirmed the live `/exec` URL still resolves correctly afterward. **Going forward, publishing an Apps Script change no longer requires opening the online editor** — it's `clasp push` + `clasp deploy -i <that deployment ID>` from the `apps-script/` folder. Commits `1a3448c`, `9e2e4a7`, `b4572c6`. **Decision:** Mark is shifting his primary workflow to planning *and* building inside Claude Code (using plan mode), reserving Claude chat for away-from-computer brainstorming and anything needing the Google Drive connectors. **Still open (not started):** Priority 2 (admin cheat sheet for new spreadsheet helpers) from the other-machine planning brief. **Minor housekeeping flagged, not yet fixed:** (a) the repo on this new machine actually lives at `~/Desktop/MY DOCUMENTS/Projects/cru-hs-60th`, not `~/MY DOCUMENTS/Projects/` as the Two-Machine Rules state; (b) two OTHER deployments exist on the same script (`@HEAD`, and `@2 - Dropdowns`) that weren't investigated — purpose unconfirmed, left untouched; (c) `apps-script-reference.gs` (repo root) and `apps-script/Code.js` are now duplicates of the same content — worth retiring the older one later, not done yet.
>
> **Latest (2026-07-01): three share.html tester fixes — SHIPPED, live, NO redeploy.** From tester feedback: (a) the event picker now re-fetches with a cache-buster and reloads at the verify step + on "Share another", so a just-suggested event (e.g. "Spring Missions") shows up for the NEXT submitter instead of a stale cached list (the value was always saving fine; only the read was stale). (b) Added "can't be edited after submitting" notes at the photo step and the final review step, so people add their photo before sending. (c) Renamed the service-history **"Map my journey" button to "Continue"** (the real submit stays "Submit my service history") to reduce older-user confusion. All front-end in `share.html`; verified live on highschoollegacy.crutastic.com. Commit `771cddc`.
>
> **Latest (2026-06-30): search bars + event-card descriptions + boolean-Approved fix — SHIPPED & DEPLOYED (owner confirmed the redeploy).** Live **search bars** on the People and Locations tabs (filter cards as you type). Event/collection cards now show a borrowed **description snippet** on the card face (**parked idea #1 — done**). The intake/append scripts now write a real **boolean FALSE** (not the string `"FALSE"`) for Approved on new submissions, on BOTH the **Stories** and **Staff Service** tabs, so the checkbox renders unchecked (**parked idea #3 — done**). **Owner pasted + redeployed the Apps Script on 2026-06-30, so the boolean-Approved fix is LIVE.** Also: added a **"Two-Machine Rules"** section to CLAUDE.md + HANDOFF.md and moved the repo OUT of Google Drive to `/Users/markmichal/MY DOCUMENTS/Projects/cru-hs-60th` (plain local folder — Drive was corrupting `.git`). Commits `b2ee024`, `bafdd04`, `a1b6e45`, `e1a859d`, `b3a73e1`.
>
> **Latest (2026-06-29): Events Grouping — SHIPPED (DEPLOYED, no owner action remaining).** Items that share an `Event` tag now collapse into one rich event card in BOTH the timeline and the Stories gallery — a bold stacked-deck card with a gold "{n} items" pill and photos rotating ~2s — and clicking it opens a single-scroll pop-out listing each item's photo + story + people + type. A site-wide photo lightbox (event pop-out AND the single-card modal) opens any real photo full-size, with a "View full image" link to a larger Drive size. `share.html` gained an event picker (existing names pulled live from both tabs + "Suggest a new event"). The owner added the `Event` column to both tabs and pasted + redeployed the Apps Script Event-write, so `apps-script-reference.gs` matches the live file (commit `b1b15f3`). See "2026-06-29 — Events Grouping (SHIPPED)" for the full breakdown and "Parked / future ideas" for the five deliberately-deferred follow-ups.
>
> **Latest (2026-06-27):** the share.html media input was overhauled (auto-detect pasted links, either/or one-media-per-story), **all videos now embed in-site including Vimeo**, follow-ups fixed **Vimeo input thumbnails** + a **verify-step media preview**, added a server-side **`vimeoThumb` proxy** (unlisted-Vimeo thumbnails), and made the story pop-out **never show a third-party embed error** — a Vimeo with embedding disabled now shows a warm "▶ Watch this video" card instead of Vimeo's black privacy-error box. The front-end is live on the Pages rebuild and **degrades gracefully**. See the four "2026-06-27" sections below. **One owner action covers it all:**
> - **Redeploy `apps-script-reference.gs`** (the `vimeoThumb` proxy + its new `embeddable` flag) to switch on (a) unlisted-Vimeo input thumbnails and (b) inline playback for embeddable Vimeo in the pop-out. It bundles with the already-pending redeploy below — no extra trip. **Until you redeploy, all Vimeo videos in the pop-out show the "Watch this video" card** (never an error); YouTube/Drive are unaffected.
>
> **Latest (2026-06-28): type system cleanup — front-end only, NO Apps Script change, NO redeploy.** Cleaned up the item "type" system in `index.html` + `share.html`. **"Event" is no longer a type** (it becomes a grouping tag in a later build) and **"Story" is a real type again** (text-only submissions). Final filter buttons: **Milestone · Photo · Flyer · Story · Video**. On `share.html` the Type is **auto-assigned** from what they add (PDF→Flyer, image→Photo, video file/link→Video, no media→Story) and written to the Type column by header name. The server already writes the client's type by header name (`normType`, whose allowlist already includes Story/Flyer), so **no `.gs` edit and no redeploy** were needed. Existing **Event-typed rows keep displaying** (under "All"/their decade, just no chip); existing **Story-typed rows stay Story**. **Two same-day refinements (2026-06-28 later):** (a) the Stories gallery now **always shows all five chips** — Milestone · Photo · Flyer · Story · Video — even when no item of that type exists yet (an empty type just yields a "no items" result); (b) the share.html verify-step Type is now an **editable dropdown** (Photo/Flyer/Story/Video, not Milestone) that **defaults to the auto value but the submitter can override** — so a flyer that scanned as a Photo can be switched to Flyer. **Owner action: just eyeball the live site** after the Pages rebuild (all five chips present; verify-step Type dropdown defaults correctly and is changeable). See "2026-06-28 — type system cleanup" and "2026-06-28 (later) — type system refinements". **Reminder: do NOT run `convertStoryTypeToPhoto()` (now obsolete).**
>
> **Rename (2026-06-27): "High School History" → "High School Legacy" + new domain.** Code changes are done (CNAME, og/twitter/title meta, header `<h1>` default). **No Apps Script change, no redeploy.** Two owner actions OUTSIDE the repo: (1) **DNS** — point `highschoollegacy.crutastic.com` at GitHub Pages and set it as the custom domain in repo Settings → Pages (the old `highschoolhistory` host stops resolving once DNS moves). (2) **Hidden editor (PIN 6060)** — if a saved `headerTitle` exists in Site Settings it OVERRIDES the HTML default, so update it to "Cru High School Legacy" there or the header may still read "History". See "2026-06-27 — rename to High School Legacy".
> Prior state (still true): the owner did the `Form Responses 2` → `Stories` tab rename and the Stage 1.2 / 1.3 / structural-pass redeploys, so those server changes are live.

## Two-Machine Rules

- Repos must live in a plain local folder, never inside a Google Drive-synced folder — Drive can corrupt `.git` with conflict-copy files.
- Both repos currently live at `/Users/markmichal/MY DOCUMENTS/Projects/`.
- Run `git pull` at the start of every session, on whichever machine you're on.
- Commit and push before stopping so the other machine can pick up current work.
- `.env` and `.claude/` must always be in `.gitignore` and must never be committed.
- Each machine needs `git config --global user.name` and `user.email` set to `Mark Michal` / `mark.michal@cru.org` so commits attribute correctly.

## 2026-07-02 — new machine onboarded; DOCTYPE error was a stale browser cache; Session Rhythm added; Priority 1 (Clasp) shipped
Mark started using a brand-new MacBook for this project. Session covered getting it fully working, chasing down a false-alarm production scare, adding two standing process rules to `CLAUDE.md`, and — in a second half of the same session — setting up `clasp` end-to-end and shipping Priority 1 from the other-machine planning brief.

- **New machine setup.** `git config --global user.name/user.email` set to `Mark Michal` / `mark.michal@cru.org` (was blank on this Mac). GitHub push access was missing entirely (no saved credentials, no SSH key, no `gh` CLI) — Mark generated a GitHub personal access token and it's now stored in the macOS keychain via `git credential approve`, so `git push`/`git pull` work without prompting. **Housekeeping note:** since the token was shared in a chat message, Mark should eventually revoke/regenerate it at github.com/settings/tokens as routine hygiene — not urgent, nothing is broken by leaving it.
- **The "form is broken" investigation.** Mark got `Sorry, something went wrong: Unexpected token '<', "<!DOCTYPE "... is not valid JSON` while saving service history on `share.html`. Traced the error message to `share.html`'s `friendly()` helper — it fires whenever `postAction()`'s `res.json()` chokes on an HTML reply instead of JSON. A direct probe of the live Apps Script's POST path (via `curl`, following the redirect) returned `405` + an HTML "Page Not Found" page for every action, including harmless read-only ones — which looked exactly like a broken/out-of-sync deployment (the kind the "Owner actions" notes throughout this file usually call for). **However, the real cause was local to Mark's new-machine browser: a hard browser reset fixed it immediately, and the site was working fine for other visitors the whole time.** The `curl` probe still returns the 405 HTML even now that the browser works — so that probe method is not a reliable stand-in for a real browser request against this Apps Script setup; don't trust it alone next time without also trying a hard refresh first.
- **`CLAUDE.md` — Session Rhythm (new, standing rule).** At the START of every session: `git pull`, read `HANDOFF.md`, then give Mark a 2–3 sentence orientation summary — before doing anything else. At the END of every session: add a dated `HANDOFF.md` entry (what was built/decided, new parked items, open threads), commit, push, and tell Mark "HANDOFF.md updated and pushed" — before stopping. Both automatic, no need for Mark to ask.
- **`CLAUDE.md` — "Working with Mark" (new, standing rule).** Mark has no coding background. Explain in plain language, avoid/define jargon, help him think and plan (not just execute), break work into small steps with check-ins, and prefer agreeing on the approach before changing code. This preference is also saved in Claude's cross-project memory (not just this repo) so it applies to any future project, not only this site.
- **Decision: primary workflow shifts to Claude Code.** Mark had been planning in Claude chat, then relaying prompts into Claude Code and shuttling results back and forth — tedious and detail-lossy. Going forward, planning AND building both happen here (using plan mode so Mark approves the approach first). Claude chat is now reserved for two cases only: brainstorming away from the computer, and anything that needs the Google Drive connectors (which Claude Code doesn't have).
- **Priority 1 — Clasp setup — ✅ SHIPPED.** Installed Node.js (this new Mac had neither Node nor Homebrew) and `clasp` (Google's Apps Script CLI) into a user-owned npm folder (`~/.npm-global`, to avoid needing `sudo`), then `clasp login` (one-time Google OAuth in the browser). Got the live script's Script ID from Mark (Apps Script editor → Project Settings) and ran `clasp clone` into a scratch folder as a safety check first — **this surfaced that the live script actually has 9 files, not the 1 the repo tracked.** `Code.js` matched `apps-script-reference.gs` exactly (only a trailing-newline difference), but `appsscript.json`, `Dropdowns.js`, `corrections.js`, `Reformat.js`, `checkboxes.js`, `newform.js`, `newform2.js`, `newform4.js` existed ONLY inside Google — no backup anywhere. Important gotcha caught before it caused damage: `clasp push` replaces the *entire* file set on the live script, so pushing with only 1 of 9 files tracked would have **deleted the other 8 from the live script** the first time anyone used it. Fixed by copying all 9 (+ the `.clasp.json` pointer) into a new `apps-script/` folder in the repo (see `apps-script/README.md`), scanned for secrets before committing (none found — one comment mentions the `sk-ant-` key *format*, not an actual key), committed and pushed (`b4572c6`). Then found the live deployment ID via `clasp deployments` (`AKfycbwNrxCjE2WFOLNxkwVzNblEeRN2q7qyVbz_AqAexL1ho5U0GtPX6meS62fhcCN5wu0y`, confirmed against the `SCRIPT_URL` used in all 4 HTML pages — was `@19`), and proved the full workflow with a no-op `clasp push` + `clasp deploy -i <that ID>` → published as `@20`, then verified the live `/exec` URL still resolves. **From now on, publishing an Apps Script change is two terminal commands run from `apps-script/` — `clasp push` then `clasp deploy -i AKfycbwNrxCjE2WFOLNxkwVzNblEeRN2q7qyVbz_AqAexL1ho5U0GtPX6meS62fhcCN5wu0y` — no more opening the online editor.** Two loose ends, not investigated: an `@HEAD` deployment and a separate `@2 - Dropdowns` deployment exist on the same script; purpose unconfirmed, left untouched. Also: `apps-script-reference.gs` (repo root) and `apps-script/Code.js` are now duplicate copies of the same content — worth retiring the older one in a future cleanup, not done yet.
- **Priority 2 — Admin cheat sheet — still not started.** A one-page plain-language guide for new non-technical spreadsheet helpers (tabs, Type rules, Approved/On Timeline/Featured, Event tag spelling, what not to touch). Next up whenever Mark wants to tackle it.
- **Minor doc inconsistency flagged (not fixed):** the Two-Machine Rules section says repos live at `/Users/markmichal/MY DOCUMENTS/Projects/`, but on this new Mac the actual path is `/Users/markmichal/Desktop/MY DOCUMENTS/Projects/cru-hs-60th`. Not currently a problem (nothing hardcodes the path), but worth tidying up next time this doc is touched.

## 2026-07-01 — three share.html tester fixes (front-end only, NO redeploy)
From a testing session. All in `share.html`; verified in a browser and confirmed live.
- **Event picker read was stale, not the write.** A suggested event (e.g. "Spring Missions") saved to the `Event` column correctly, but the next submitter didn't see it. Cause: `loadEventOptions()` ran only once at page load, with no cache-buster, so the browser/gviz served a stale list. Fix: `fetchEventNames()` now appends `&_cb=${Date.now()}` to the gviz URL; `loadEventOptions()` preserves the current selection and is now also called (a) when the verify/preview step is shown and (b) on "Share another". Verified live: the dropdown shows all five events including Spring Missions, no console errors.
- **"Can't edit after submitting" notes.** A tester asked whether she could go back and add a photo later. Added two `dim-note` lines: one at the photo step ("Best to add it now… no adding a photo later") and a highlighted one above the final Share button ("…can't be edited, so this is the time to include a picture").
- **"Map my journey" → "Continue".** That button is the *parse-into-a-review-table* step, not the submit; renaming it "Submit" would clash with the real "Submit my service history" button below. Chose "Continue" (Mark picked it). Also updated the button's busy-state text to "Reading your journey…" and its reset label.

## 2026-06-30 — search bars, event-card descriptions, boolean-Approved fix, repo relocation
Done on Mark's other machine; all pushed. Front-end verified live; the Apps Script change was pasted + redeployed by the owner the same day (confirmed 2026-07-01).
- **Live search bars (People + Locations tabs).** `peopleSearch` / `locationSearch` inputs filter their cards as you type. Front-end in `index.html`.
- **Event-card description snippet (parked idea #1 — DONE).** Event/collection card faces now show a borrowed description (Featured item's story if present, else earliest), truncated (`ev-desc`), so the timeline reads better.
- **Boolean Approved on new submissions (parked idea #3 — DONE, DEPLOYED).** The intake/append handlers now write a real boolean `false` (was the string `"FALSE"`) for Approved, on BOTH the Stories and Staff Service tabs, so the checkbox renders unchecked and conditional formatting still fires. This is an Apps Script change — **the owner pasted + redeployed on 2026-06-30, so it is live.** `apps-script-reference.gs` matches.
- **Two-Machine Rules + repo moved out of Google Drive.** Added a "Two-Machine Rules" section to `CLAUDE.md` (and here): the repo now lives at `/Users/markmichal/MY DOCUMENTS/Projects/cru-hs-60th` (plain local folder, NOT Drive — Drive was corrupting `.git`); `git pull` at the start of every session and commit+push before stopping so the other machine stays current.

## 2026-06-29 — Events Grouping (SHIPPED)
The full Events Grouping feature is live: front-end in `index.html` + `share.html`, plus one Apps Script change that the owner pasted and **redeployed**. Items that share an `Event` tag collapse into one rich event card/pop-out across the whole site. **No owner action remaining for this feature.**

- **Reader + grouping.** `rowsToItems()` reads the new `Event` column (by header name) into a trimmed `event` field on every item, covering BOTH tabs. `collapseEvents()` takes each view's **already-filtered** item list and returns a mix of single cards + event groups (2+ items sharing a non-blank `Event` value; a lone tagged item stays a single card). Group year = the agreed year, else the **earliest** (`console.warn` flags disagreement). Wired into both render paths on their post-filter lists — `renderTimeline` (`timelineItems()`) and `renderGallery` (`applyFilters()`) — so the "{n} items" count and resolved year reflect what that view shows (an event with one On-Timeline item is a single card on the timeline but groups fully in the gallery).
- **Event card visual.** Bold **stacked-deck** look (real sibling `.ev-layer` elements inside a `.ev-stack` wrapper — always visible at rest, peeking down-right), a gold "{n} items" **count pill** with an inline stacked-layers icon, and the photo face **rotating ~2s** through the group's photos (crop-to-fill via `object-fit:cover`, one global `startEventRotators` ticker, **paused under `prefers-reduced-motion`**). Face text = event name + year + count only.
- **Event pop-out.** Clicking an event card opens ONE scrolling pop-out (reuses the existing modal overlay; **no nested modal**) — event title + year on top, then each item with its own photo + that item's story + people + a **per-item type label** (same `.tag.type-*` styling as the single-card modal). `openEventModal()` shows every approved item whose `Event` matches (case/space-insensitive).
- **Photo lightbox, site-wide.** A lightweight `#imgLightbox` overlay (z-index above the modal) opens any real-photo image full-size — from the event pop-out AND the regular single-card modal. Backdrop / × / Esc-first close it without disturbing the underlying modal (scroll preserved). A **"View full image"** link opens a larger Drive size (`driveImgSize`, `sz=w2400`) in a new tab. Only real photos carry `.lb-open`; video thumbnails and text-only items don't trigger it.
- **share.html event picker.** The verify step has an Event dropdown of existing event names (pulled live via gviz from the **distinct, non-blank `Event` values across BOTH tabs** — master + Stories) plus a "Suggest a new event" free-text option. The fetch is non-blocking; if it fails the free-text path still works. The chosen string is sent as `story.event`.
- **Apps Script Event persistence (DEPLOYED).** `handleAddStoryFromPublic` reads `story.event` and writes it to the Stories `Event` column **by header name** (`M.colOf("event")` — never created here; blank/missing writes an empty cell; if the column is absent it fails soft and writes the rest). The owner pasted the full file and redeployed a new web-app version. `apps-script-reference.gs` matches the live `Code.gs` (commit `b1b15f3`).

## Parked / future ideas (do NOT build without a go-ahead)
*Deliberately deferred during the Events Grouping work — re-parked 2026-06-29. Update: #1 and #3 were completed 2026-06-30 (marked below); #2, #4, #5 remain not started.*

1. ✅ **DONE 2026-06-30 — Timeline event-card description.** The event card face now shows a *borrowed* description (Featured item's story if present, else earliest, truncated — `ev-desc`). Optional later: a custom per-event description field.
2. **Stronger visual differentiation for event/collection cards.** The current bold-stack + gold pill still doesn't read as clearly "different." Explore a visible **"Collection" label**, a **tinted background or colored band**, and a **hover lift**. Design-by-mockup with Mark first.
3. ✅ **DONE 2026-06-30 (DEPLOYED) — boolean Approved on new submissions.** The intake/append scripts now write a real boolean `false` (not the string `"FALSE"`) for Approved on new rows, on BOTH the Stories and Staff Service tabs, so the checkbox renders unchecked and conditional formatting still fires. Owner redeployed the Apps Script the same day. *(Remaining, if ever wanted: applying checkbox data-validation formatting to EXISTING rows is a manual Sheet task.)*
4. **Cleanup: normalize EXPLO '72 spellings** to one identical `Event` value so those rows group together.
5. **Cleanup: reassign leftover `Event`-typed rows** to a real Type, now that "Event" is a grouping tag and no longer an item type.

## Status as of 2026-06-18

**Current direction: working from GitHub (this repo). Apps Script "Site 2" is shelved for now — but fully built and preserved, so it can be revived if Cru policy later requires all-Google hosting.**

There are two working versions of the site (same Google Sheet as the data source):
1. **GitHub Pages** (this repo) — the active/public site, fast, clean custom domain, no banner. **All new work happens here.**
2. **Apps Script "Site 2"** — an all-Google-Workspace port (separate project, see below). Fully working but **shelved for now**; kept as a fallback in case Cru policy requires hosting to stay inside Google.

---

## Completed — GitHub Pages site (this repo, `index.html` unless noted)

- **Phase 1 — Staff Service & Locations view**: Locations nav tab, Staff Service tab loading, People profiles with service-journey chips, location detail pages with era-grouped rosters.
- **Phase 2 — Staff AI intake** (`intake.html`): PIN-gated (1951), paste notes → Claude parse → editable preview → append to Sheet with Approved=FALSE.
- **Locations map**: merged into the Locations tab — Leaflet/OpenStreetMap on top, city cards below; auto-geocoded via Nominatim.
- **History intake** (`history-intake.html`): PIN-gated (1951), for Tom — freeform ministry-history notes → Claude parse → editable preview (Title, Year, Location, People, Type, Story, On Timeline) → append to master timeline tab, Approved=FALSE. Story field is a full-width auto-growing row.
- **Apps Script reference** (`apps-script-reference.gs`): actions `saveSettings`, `parseNotes`, `addStints`, `parseHistory`, `addHistoryEvents`, plus photo auto-renaming (form-submit trigger + "Cru 60th" menu). Claude Haiku 4.5 via Anthropic API; key in Script Properties.
- **Hero redesign**: split layout (title/text beside the photo carousel, no text over the image); caption is just an outline "View this photo" button.
- **Clickable everywhere**: people, locations, and years in cards and the detail modal all navigate to related things (person profile / location page / year-filtered gallery).
- **Gallery search includes the year**; person profile's summary date/location "trail" removed (service journey is the single source of truth).
- **Social share preview**: Open Graph + Twitter Card meta tags.
- **Custom domain**: `CNAME` → `highschoollegacy.crutastic.com` (renamed 2026-06-27 from `highschoolhistory.crutastic.com`; the `markmichal.github.io/cru-hs-60th/` URL still works too).

### GitHub site — owner actions still needed
1. Redeploy the live Apps Script from `apps-script-reference.gs` (Manage deployments → New version) so the History intake + photo-renaming actions are live.
2. Confirm `ANTHROPIC_API_KEY` is in Script Properties.
3. Smoke-test `intake.html` and `history-intake.html`.

### GitHub site URLs
- Site: https://highschoollegacy.crutastic.com (also https://markmichal.github.io/cru-hs-60th/)
- Staff intake: `/intake.html` · History intake: `/history-intake.html`

---

## Completed — Apps Script "Site 2" (the all-Google port)

- **Lives outside this repo**: a clasp project in a separate local folder (`~/cru-hs-60th-site2/`), managed with `clasp` (logged in as mark.michal@cru.org). Apps Script Script ID `1jaFv4uKKthgAKbNsyEImYIkI921CBPMxtcwY28ryYphLIoiiLAyVyxpJ`. Files: `Code.js` (server), `index.html`, `appsscript.json`.
- **Public URL** (now "Anyone, even anonymous"): `https://script.google.com/macros/s/AKfycbw21uOCZFyrj3dYPCyicleT_TzcxAt-yRNfdK46XaRt-pr2Mtdsmv2K_YIMBqFfcCko/exec`
- **Architecture**: page runs in the Apps Script sandbox, so it can't fetch the Sheet/geocoder directly — the server (`Code.js`) bridges it. `fetchAllData()` reads all four tabs in one parallel call; `geocodeLocations()` caches coordinates in Script Properties (persistent, shared); `doGet()` embeds the data into the page (templating) so there's no startup round-trip.
- **CRITICAL gotcha (cost us hours)**: Apps Script's HtmlService runs an old comment-stripper that does NOT understand ES6 template literals — it treats `//` inside *any* script string as a line comment and deletes the rest of the line. So **every `https://` (or any `//`) inside the inline `<script>` must be written `https:\/\/`**. Embedded data is placed as escaped TEXT in a hidden `<div>` (not inline JS) to dodge this.
- **Known quirks (inherent to Apps Script, not bugs)**: Google's "created by another user" banner shows for public visitors (unavoidable); no custom domain (long script.google.com URL); a small extra load latency vs GitHub Pages.

---

## In progress / pending decision
- **Hosting decision is with Cru (policy, not technical)**: can the public site live on GitHub Pages (outside Google), or must it stay inside Google (use Site 2)? Both versions are built and working, so we're not blocked either way.
- **Current lean (as of 2026-06-18): proceed on GitHub Pages.** Site 2 is likely to be abandoned for now, but is preserved and revivable if the policy answer changes. Treat GitHub as the source of truth for ongoing work.

---

## Completed — `share.html` (public story page, Stage 1)
A **public, photo-first AI story submission page** — no PIN. Single static page in this repo, same dependencies/brand as the intake pages but warmer, with big touch targets and large text for older users on tablets. Live path: `/share.html`. Top-to-bottom:
- **Welcome-video bar** — slim strip with a thumbnail + play overlay; opens the video in a lightbox (closes on X / backdrop / Esc). Reads `welcomeVideoUrl`, `welcomeVideoThumb`, `welcomeVideoLabel` from the **Site Settings** tab; if `welcomeVideoUrl` is empty the whole bar is hidden. These three keys were added to the hidden editor (`SETTINGS_SCHEMA` in `index.html`, section "Welcome video (Share page)") so they're editable without code.
- **Collapsed service-history panel** — name field + textarea (with mic) → "Map my journey" (`parseServiceText`) → editable Location/Start/End/Role table → "Submit my service history" (`addServiceFromPublic`) appends to **Staff Service** (Approved=FALSE, Person Name + Submitted By = their name). Collapses to a ✓ on success.
- **Media hero** — big "Add a Photo" upload (image/video) OR "paste a link". Uploads go through `uploadStoryMedia` → saved to a Drive **"Story Uploads"** folder → returns a `driveImg()`-compatible link. Shows an inline preview.
- **Story** — warm textarea + a mic button that uses `webkitSpeechRecognition`, shown **only** where the browser supports it (feature-detected; hidden otherwise — e.g. Firefox).
- **Verify + submit** — "Share this story" (`parseStoryText`) → editable preview (Title/Year/Location/People/Type/Story) → final submit (`addStoryFromPublic`) appends to **Form Responses 2** (Approved=FALSE, On Timeline=FALSE, Submitted By = name). Photo link → Photo column; video link → Video column + a "Watch the video" button. Encourages-but-never-requires a photo (gentle nudge with a "Continue without a photo" option).
- **Apps Script** (`apps-script-reference.gs`): five new public actions added (`parseServiceText`, `addServiceFromPublic`, `uploadStoryMedia`, `parseStoryText`, `addStoryFromPublic`), all using Claude Haiku 4.5 + the existing `ANTHROPIC_API_KEY`. All existing actions intact. No PIN on the public actions; text/file-size caps as light abuse guards.

**Verified locally:** page loads with no console errors; the mic shows when supported and hides when not; the welcome bar stays hidden while `welcomeVideoUrl` is empty; service expand, video-link detection, and the no-photo nudge all work.

### Stage 1.1 refinements (2026-06-23)
- **Name + email now required** to send a story. Added a "Your email" field beside "Your name" in the story area; both are hard-required (clear inline red errors block "Share this story" until name is filled and email looks valid — contains `@` and a domain). Year/location/people are NOT required.
- **Email is stored.** `addStoryFromPublic` writes the submitter's email to an **Email** column in Form Responses 2 (created if missing).
- **Server-side timestamp.** `addStoryFromPublic` stamps the current date/time (generated in Apps Script, never the browser) into the existing **Submitted At** and/or **Timestamp** column, matched by header substring. Service rows already stamp Submitted At server-side.
- **Pre-fill on repeat.** "Share another" no longer reloads — it clears the story/photo/preview but **keeps the name + email** filled for the session (in-page state; no localStorage, which is blocked here).
- **Red attention-prompts on the verify step** for Year / Location / People: a soft muted helper sits above each field always; on a submit attempt with any empty, those turn **red + insistent** and the box is flagged, and the button becomes "Share anyway". The red clears the instant they type. These three never block submission (the story still goes through).
- Verified locally: required name+email gating, invalid-email block, error-clear-on-type, the encourage red prompts + "Share anyway", and name/email pre-fill on "Share another" all work; no console errors.

### Stage 1.2 refinements (2026-06-23)
- **Verify-step layout cleanup (share.html).** On the "Does this look right?" preview, every label now sits directly above its input and the two columns line up (Title/Year row and Location/People row). Removed the icon + above-box helper sentences (calendar/pin/people emoji) for Year/Location/People. The guidance now lives as light-gray placeholder text **inside** each box — Year: "Tell us the year if you can"; Location: "Where did this happen?"; People: "Names, separated by commas".
- **Red empty-prompt kept, just cleaner.** If Year/Location/People are empty on a submit attempt, a clean red prompt appears **below** the box (no icons) and the box is flagged, the button becomes "Share anyway", and the message bar still says it's fine to send as-is. Still never blocks submission; the red clears the instant they type. Verified locally (preview): alignment, placeholders, red prompts on empty-submit, clear-on-type, and "Share anyway" all work; no console errors.
- **Column mapping fix (Form Responses 2).** `addStoryFromPublic` now writes the submitter's NAME to the form's existing **"Your name"** column and the server-side timestamp to the form's existing **"Timestamp"** column (both matched by header substring). It **no longer** writes separate "Submitted By" / "Submitted At" columns on Form Responses 2 (those duplicated the form's own columns). The **Email** column and server-side timestamping behavior are unchanged. **Staff Service is untouched** — its Submitted By / Submitted At stay as intentional provenance.
- **Type dropdown: Video replaces Story (share.html).** The verify-step Type select now lists **Photo / Flyer / Video / Event** (was …/Story/…); the AI-parse fallback defaults to Photo. Added "Video" to `EVENT_TYPES` in the Apps Script so a manually-chosen Video persists through `normType` instead of collapsing to "Event" (Story is kept server-side for the history-intake/timeline path).
- **Site CTA buttons now point to share.html.** Both "Share Your Story" submit buttons (header `submitBtn` + footer `footerSubmit`) in `index.html` now go to our own in-site story page instead of the old Google Form. `CONFIG.FORM_URL` default is now `share.html`, and a new `storySubmitUrl()` helper **ignores any saved Google-Form URL** in the Site Settings tab so the buttons always land on share.html (the live setting still pointed at the old form — this neutralizes it without needing an editor change). Internal pages navigate in the **same tab**; an external custom URL would still open in a new tab. The **"Suggest a correction" button is unchanged** (still its own correction Google Form). No Apps Script redeploy needed for this — it's all client-side in index.html.

### Stage 1.3 — structural pass (2026-06-23)
*A coordinated code + Sheet change. The code is live in index.html; the owner must rename the Sheet tab and redeploy the Apps Script — see the checklist below.*
- **Tab renamed "Form Responses 2" → "Stories".** The site reads story/gallery rows from this tab. Updated **everywhere**: `CONFIG.FORM_TAB` in `index.html` (the gviz fetch), `FORM_TAB` in `apps-script-reference.gs` (story writes + the photo-renamer), and all doc comments. No other page references the tab name (the intake pages POST actions handled server-side). The owner renames the actual Sheet tab by hand; the code now looks for **"Stories"**.
- **All writes are now BY HEADER NAME, never by position.** This was the core bug — the owner had moved Approved / On Timeline / Featured to the far left, so position-based writes landed names in the Approved column. A shared `colMapper_()` helper reads each tab's header row and finds every target column by case-insensitive substring. Applied to **every** intake/append handler: staff intake (`addStints`), history intake (`addHistoryEvents`), and the public share.html actions (`addServiceFromPublic`, `addStoryFromPublic`). The owner can now reorder columns freely without breaking writes. (Reads in index.html — `rowsToItems`/`rowsToStints` — were already header-name based.)
- **New submissions insert at the TOP (row 2), newest first**, on Stories, Staff Service, and the master timeline (shared `insertTopRows_()`), so the newest items are visible first in the Sheet.
- **Approved defaults to the literal "FALSE"** (not blank, not a boolean) on every new incoming row, plus **On Timeline = FALSE** and **Featured = FALSE** where those columns exist — so the owner's red conditional formatting flags new rows as needing review. (`addStoryFromPublic` writes Approved/On Timeline/Featured = "FALSE"; the timeline keeps the parsed On Timeline value as "TRUE"/"FALSE".)
- **No tabs deleted** — the owner handles tab cleanup. Verified locally: `apps-script-reference.gs` passes a JS syntax check; index.html loads the live Sheet with no console errors and `CONFIG.FORM_TAB === "Stories"`.

### ⭐ Owner checklist for the structural pass (do in this order)
1. **Rename the Sheet tab** from **"Form Responses 2"** to **"Stories"** (double-click the tab name at the bottom of the spreadsheet). Exact spelling, no trailing spaces.
2. **Paste the updated `apps-script-reference.gs`** into Extensions → Apps Script (select all, delete, paste, Save), then **Deploy → Manage deployments → New version → Deploy** (same web app URL).
3. **Confirm the gallery still loads** at https://highschoollegacy.crutastic.com after the rename (the "Live from Google Sheets — N approved items" status should still appear and show your story items).
4. **Submit one test story** via `/share.html` and confirm the new row appears **at the top (row 2)** of the Stories tab, with **Approved = FALSE** (red), and the name/timestamp in **Your name** / **Timestamp**.
5. (Optional) Test the staff/history intakes and the public "Map my journey" the same way — new rows should also land at the top with Approved = FALSE.
6. Reorder columns however you like — writes will keep landing in the right columns by header name.

### share.html — owner actions still needed
1. **Redeploy the Apps Script** from `apps-script-reference.gs` (Manage deployments → New version) so the public actions — **including the email + server-timestamp behavior and the new Your name / Timestamp column mapping** — go live. Until then the page's buttons will error. Same web app URL. (See the dated ">>> CHANGED 2026-06-23" note at the top of the .gs.)
   - **After redeploying**, submit one test story and confirm it lands in the **Your name** + **Timestamp** columns of the **Stories** tab. Then you may **delete the now-unused "Submitted By" / "Submitted At" columns** from Stories — they are no longer written or read for that tab (index.html never displays them; only Staff Service uses those header names, on its own tab). Leave Staff Service's columns alone.
2. First photo upload creates the **"Story Uploads"** Drive folder and prompts for a Drive permission — approve it. Then set that folder's sharing to **Anyone with link → Viewer** (see SETUP note F at the top of the .gs).
3. Optionally set `welcomeVideoUrl` (+ thumb/label) in the hidden editor (PIN 6060) to switch on the welcome bar.
4. Smoke-test `/share.html` end to end after redeploy.

### Stage 2 (2026-06-24) — DONE (pending redeploy)
Photo/video **auto-renaming** for public share.html uploads now mirrors the existing form-submit renamer. Implemented entirely in `apps-script-reference.gs`:
- A new `renameStoryUpload_()` helper renames the just-uploaded Drive file to the same clean **"Year — Title — People.ext"** convention the Google-Form path uses (reusing the existing `buildPhotoName` / `renamePhotoFile` / `driveFileIds` / `fileExt` helpers).
- It's called at the **end of `addStoryFromPublic`**, *after* the row is written (so a rename hiccup can never lose a submission) and wrapped in try/catch. The rename is done at submit time, not upload time, because the Title/Year/People aren't known when `uploadStoryMedia` first saves the file.
- Only files in the owner's **"Story Uploads"** Drive folder are touched; pasted **YouTube/external links no-op** (`driveFileIds` finds no ID to rename). Works for both photo and video uploads (`fileExt` now also maps mp4/mov/webm).
- **No new trigger or permission** beyond the Drive access already approved for "Story Uploads". No index.html/share.html change — server-side only.
- Verified locally: `apps-script-reference.gs` passes `node --check` (via a .js copy). **Owner action: one redeploy** of the Apps Script (Manage deployments → New version, same URL) to make it live — see the ">>> CHANGED 2026-06-24" note at the top of the .gs. After redeploy, submit a test story with a photo on `/share.html` and confirm the file in the "Story Uploads" Drive folder is renamed to "Year — Title — People".

*Remaining:* any polish from real-world testing.

## 2026-06-25 (later) — three front-end tweaks (no redeploy)
Pure front-end, `index.html` + `share.html` only. No Apps Script change, no redeploy — these go live on the next Pages rebuild with no owner action beyond eyeballing.
- **share.html "Share another" scroll fix.** The share-another handler ended with `window.scrollTo({top:0})` followed by `$("storyText").focus()` — the focus call yanked the viewport back down to the story textarea. Replaced both with `$("mediaCard").scrollIntoView({block:"start"})` and removed the focus, so clicking "share another" lands at the TOP of the "Share a photo, flyer, or video" section (the Add a Photo button), matching the form's natural top-to-bottom flow.
- **Gallery/Stories filter-bar layout.** Top-to-bottom order is now: decade bar → three equal-width controls → type buttons → cards. The `.filter-bar` is a 3-column CSS grid (`repeat(3,1fr)`) holding the search box, "All locations", and "All people" — each exactly one-third, stacking to full width below 560px. The search placeholder is shortened to "Search titles and stories…" (it still searches location/people/year under the hood). The type chips moved to their own `.type-chips-row` below the three controls, with "Clear all" pushed to the right of that row. The decade bar (`#galleryDecades`) was moved above the controls to match the requested order.
- **Logo badge 50% larger.** `.badge-60 img` went from `width/height: 66%` to `95%`. Safe because the cru+ artwork has generous built-in whitespace, so the visible mark still sits cleanly inside the circle with padding.

*Verified:* both files' inline `<script>` blocks parse clean (vm); landmark grep confirms each change and that the only remaining `storyText.focus()` is an unrelated empty-field validation. Verified statically — the Claude Preview panel is rooted at a different project, so eyeball the live site after the Pages rebuild.

## 2026-06-25 — front-end batch + identity shift
A batch of `index.html` front-end changes (modify-not-rebuild) plus one `apps-script-reference.gs` data utility. The repo is no longer framed only as the one-time 60th anniversary; it's becoming a permanent **living history archive**.

- **Gallery decade filter bar (new).** Above the gallery cards there's now a row of decade buttons reusing the timeline's `.era-tab` styling: **"All Decades"** (default/active) plus one button per decade that actually has items (computed from `decadeOf(d.year)` over the approved `DATA`, so empty decades never show). Clicking a decade filters to items whose Year falls in it (1990s = 1990–1999); items with a blank year appear only under "All Decades". Implemented as a new `filters.decade` field (replacing the old `filters.era`), matched in `applyFilters` via `decadeOf`. It composes with the existing type/search/location/person filters. The decade buttons live in `#galleryDecades`; because they share the `.era-tab` class with the timeline, the click handler checks `e.target.closest("#galleryDecades .era-tab")` **before** the timeline's generic `.era-tab` handler and returns early, so the two don't collide.
- **Removed the "All Eras" dropdown** (`#decadeFilter` `<select>`) from the gallery filter bar — the decade buttons replace it. Its change-listener and all `$("decadeFilter")` references were removed.
- **Nav "Gallery" → "Stories".** Label only; the button keeps `data-view="gallery"` (same view/function) and `data-set="navGallery"` (still editable via Site Settings). Nav now reads Timeline · Stories · People · Locations.
- **Removed the "Story" type chip.** `buildFilterOptions` filters `Type === "story"` (case-insensitive) out of the type chips, leaving Milestone / Flyer / Photo / Event / Video.
- **`convertStoryTypeToPhoto()` (Apps Script, new).** One-time MANUAL utility — not automatic, no trigger. Walks the Stories tab (Type column matched by header name) and changes every `Story` to `Photo`. Re-runnable safely. Added to the "Cru 60th" menu too. **Owner runs it once** (see the checklist at the top). It's editor/menu-only and doesn't itself require a web-app redeploy.
- **Identity shift (#6).** Browser `<title>`, `og:title`, and `twitter:title` → **"Cru High School History"** (the "60 Years of Stories" tagline removed). Header `<h1>` default → "Cru High School History"; subtitle default → "How God has used Cru in the lives of teenagers". Title/subtitle/nav stay editable via Site Settings (caveat about saved-value overrides is in the top status block). The og/twitter **descriptions** (which mention "sixty years of moments…") were left as-is — they're descriptive prose, not the retired tagline.
- **Logo badge (#7).** The yellow circle badge now holds `cru-logo.png` (committed to the repo) instead of the "60" text — `<div class="badge-60"><img src="cru-logo.png" alt="Cru logo"></div>`, sized at 66% with `object-fit: contain` and the circle `overflow: hidden` so it sits centered with padding. The now-unused `badgeText` entry was removed from the editor's `SETTINGS_SCHEMA`.
- **Hero unchanged (#8).** The hero line "Sixty years of reaching teenagers with the gospel." is deliberately kept — sixty-years framing was only removed from the header/title/tab.
- **Verified:** all inline `<script>` blocks in `index.html` parse clean (vm syntax check); `apps-script-reference.gs` passes `node --check`; landmark grep confirms every change is present and the old dropdown/`filters.era` references are gone. The Claude Preview panel is rooted at a *different* project (the AI Research Site) and the site renders from a live Google Sheet, so this was verified by static analysis rather than the browser preview — the owner should eyeball the live site after the Pages rebuild (checklist item 3 up top).

## 2026-06-28 — type system cleanup (front-end only, NO redeploy)
Cleaned up the item **Type** system. "Event" stops being a type (it'll be a grouping tag in a later build); "Story" returns as a real type for text-only submissions. **Final type list / filter buttons: Milestone · Photo · Flyer · Story · Video.** All changes are in `index.html` (filters/display) and `share.html` (auto-assign). **`apps-script-reference.gs` was deliberately NOT changed → no redeploy.**

- **`index.html` — type filter chips are now a fixed, canonical list (`buildFilterOptions`).** Replaced the old data-driven `[...new Set(types)].filter(≠ story)` with an ordered allowlist `TYPE_ORDER = ["Milestone","Photo","Flyer","Story","Video"]`, intersected with the types actually present in `DATA` (so an empty type shows no dead-end chip, matching the decade-bar behavior). This **removes Event** (not in the allowlist) and **re-adds Story**. "Event" is intentionally excluded as a chip but the data is untouched.
- **`index.html` — type filter match is now case-insensitive (`applyFilters`).** `(d.type||"").toLowerCase() !== filters.type.toLowerCase()` so the canonical chip labels match regardless of how the Type is cased in the Sheet. When no chip is active the type clause short-circuits, so **Event-typed (and any unknown-typed) items still display** under "All Decades"/their decade — they just have no dedicated chip. Card/modal still render the Event tag (`.tag.type-Event` CSS already exists), so nothing breaks.
- **`share.html` — the submitter no longer picks a Type; it's auto-assigned.** Removed the `pvType` `<select>` (which had Photo/Flyer/Video/**Event**) and replaced it with a **read-only display** (`#pvTypeShow`, `.readonly-type`) plus a hint that we set it automatically. New `autoAssignType()`: **video file or pasted video link → Video · PDF upload → Flyer · any other media (image upload / non-video link) → Photo · no media at all → Story.** "Milestone" is never auto-assigned (team-only, set at review). `refreshAutoType()` keeps the read-only display in sync (called from `renderVerifyMedia`, which runs on every media change and when the preview opens). The AI parse's `type` field is now ignored for this.
- **`share.html` — PDF uploads now allowed (so flyers self-sort).** `fileInput accept` gained `application/pdf,.pdf`; the change handler captures `isPdf` (by mime or `.pdf` extension) into `storyMedia` and `mediaLabel()` shows "✓ Flyer added!" for it. Known/accepted limitation: a flyer *photographed or scanned as an image* auto-assigns as **Photo** and the team re-tags it to Flyer at review; **PDF** flyers self-sort to Flyer.
- **Final submit writes the auto type.** The `addStoryFromPublic` payload now sends `type: autoAssignType()`. **Server is unchanged and already correct:** `handleAddStoryFromPublic` forces `"Video"` for video media and otherwise writes `normType(s.type)` **by header name** — and `normType`'s `EVENT_TYPES` allowlist already contains both `Story` and `Flyer`, so Story/Flyer/Photo pass through verbatim. Hence **no `.gs` change and no redeploy.**
- **Existing data:** Story-typed rows **stay Story** (valid again). Event-typed rows are **left as-is** for the future events-grouping build. The `convertStoryTypeToPhoto()` Apps Script utility is now **obsolete — do NOT run it** (it would wrongly erase Story); left in the `.gs` only to avoid an otherwise-unneeded redeploy.
- **Verified statically:** both files' inline `<script>` blocks parse clean (vm); grep confirms no leftover `$("pvType")` refs, no selectable Event `<option>`, and all new helpers present. **Not browser-verified here** — the Claude Preview panel is rooted at a *different* project (it served the AI Hub / "AI Resource Center" on start, not this repo), the long-standing cross-project preview hazard — so **eyeball the live site after the Pages rebuild**: chips read Milestone · Photo · Flyer · Story · Video; an Event-typed item still appears under All/its decade with no chip; on `share.html` the verify step shows a read-only Type that flips to Story (no media) / Photo (image) / Flyer (PDF) / Video (video).

## 2026-06-28 (later) — type system refinements (front-end only, NO redeploy)
Two follow-ups to the type cleanup above. Still `index.html` + `share.html` only; `apps-script-reference.gs` untouched → **no redeploy.**
- **`index.html` — always show all five type chips (`buildFilterOptions`).** Dropped the "intersect with present types" step; `types` is now the literal `["Milestone","Photo","Flyer","Story","Video"]`, so all five chips render even when no item of that type exists yet (Milestone and Story currently have none). Clicking a type with no items just yields the existing empty/"no items" result (`renderGallery` already handles it). Type matching stays **case-insensitive** in `applyFilters`, and **Event** is still excluded (no chip) while Event-typed items keep showing under "All"/their decade.
- **`share.html` — the verify-step Type is editable again, defaulting to the auto value.** Replaced the read-only `#pvTypeShow` display with an editable `<select id="pvType">` (options **Photo / Flyer / Story / Video** — **not Milestone**, which stays team-only). `refreshAutoType()` now sets the dropdown's **value** (the auto-assigned default for the current media) instead of read-only text; it only re-defaults when the media itself changes (it's driven off `renderVerifyMedia`'s callers), so a manual override sticks through submit. The final-submit payload now sends `type: $("pvType").value` — **whatever the submitter leaves it on** is written to the Type column (server still maps it by header name via `normType`). Auto-assign defaults are unchanged (PDF→Flyer, image→Photo, video→Video, no media→Story). The hint now reads "We set this automatically from what you added — change it if needed." Removed the now-unused `.readonly-type` CSS.
- **Verified statically:** both inline `<script>` blocks parse clean (vm); grep confirms no `#pvTypeShow`/`.readonly-type` leftovers and `pvType` is wired as a select (markup → `refreshAutoType` value-set → submit reads `.value`). **Not browser-verified** — the Claude Preview panel is still rooted at a different project (served the AI Hub / "AI Resource Center", not this repo), so eyeball the live site after the Pages rebuild.

## 2026-06-27 — media input overhaul + Vimeo embedding (no redeploy)
Driven by real older users (~80) hitting friction with the media input. All client-side — **no Apps Script change needed** (the submit payload still sends `{url, isVideo}`; the server ignores the extra UI fields). Goes live on the Pages rebuild (~1–2 min); **no owner action required.**
- **Auto-detect pasted links (share.html).** The "paste a link" field has **no Add button** anymore — a recognized link is captured the instant it's pasted/typed, confirmed with a green **"✓ Link added!"** plus a **thumbnail** (YouTube: synchronous `img.youtube.com` thumb; Vimeo: fetched async via oembed, generic confirmation if it can't load; Drive: thumbnail endpoint with an `onerror` fallback to generic). The input is replaced by this confirmed state plus a **"Change link"** button that clears back to an empty field. Unrecognized links show a gentle *"Hmm, that doesn't look like a video link — try a YouTube, Vimeo, or Drive link"* — but only on blur/Enter, never mid-typing (forgiving). New helpers: `recognizeMedia()`, `fetchVimeoThumb()`, `renderMediaState()`, `tryCaptureLink()`, `clearMedia()` (replaced the old `showMediaPreview` / `looksLikeVideo` / Add-button flow).
- **Either/or — one media per story (share.html).** A story takes **either** an uploaded file **or** a pasted link, never both. `storyMedia` now carries a `source` (`'upload'`/`'link'`). When one path is in use, the other **dims/disables** with a note ("Remove your photo to paste a link instead." / "Remove the link to add a photo instead."). Removing / "Change link" re-enables the other path. This eliminates the photo-AND-video conflict.
- **Embed ALL videos incl. Vimeo (index.html display).** `videoInfo()` now also recognizes **Vimeo** (`vimeo.com/ID`, `/channels/…`, `/groups/…/videos/ID`, `player.vimeo.com/video/ID`, and private `…/ID/HASH` or `?h=HASH`) → `player.vimeo.com/video/ID` iframe; YouTube and Drive unchanged. The gallery card falls back to the row's photo when a video has no synchronous thumbnail (Vimeo). In the modal, a video link that **can't** be embedded now shows a clickable **"▶ Watch the video →"** link instead of breaking. (share.html's `videoEmbed`, used by the welcome-bar lightbox, also got Vimeo for consistency.)
- **Verified locally (Claude Preview, served from this repo):** YouTube and Vimeo paste auto-capture with the right confirmed state + either/or dimming; "Change link" resets and re-enables upload; an unrecognized link errors only on blur, not mid-typing; `videoInfo()` returns correct embeds for every YouTube/Vimeo/Drive format incl. the private hash, and `null` for non-videos; no console errors on either page.

### 2026-06-27 (follow-up) — Vimeo input thumbnails + verify-step media preview (share.html only)
Two fixes to the media preview from the build above. Still **client-side only, no Apps Script change.**
- **Vimeo input thumbnails now actually load.** Root cause: Vimeo's **oEmbed endpoint (`vimeo.com/api/oembed.json`) is CORS-blocked for browser `fetch` AND doesn't accept a JSONP callback** — confirmed live ("Failed to fetch" / script onerror), so the previous oembed approach silently produced no thumbnail. Switched `fetchVimeoThumb()` to Vimeo's **v2 "simple API" via JSONP** (`vimeo.com/api/v2/video/ID.json?callback=…`), which is not CORS-restricted and returns `thumbnail_large` — verified loading a real public Vimeo thumbnail in the preview. This covers **public** videos; **private/unlisted** ones (e.g. a `vimeo.com/ID/HASH` link) aren't in the public API, so they fall back to a clean **"✓ Vimeo video added!"** with a 🎬 video icon — **never blank** (per the ask). The fetch is async with a 6s timeout and cleans up its `<script>` + global callback; it only applies the thumb if that link is still the chosen media. YouTube thumbnails (`img.youtube.com/vi/ID/hqdefault.jpg`) confirmed still working.
- **Why not oEmbed as asked:** oEmbed can't be reached from the browser without a server proxy. The v2 JSONP API is the working client-side equivalent for public videos. *(Superseded later the same day — see the `vimeoThumb` proxy below, which Mark approved to also cover unlisted videos.)*
- **Media preview on the verify step.** The "Does this look right?" card now shows the attached photo/video thumbnail (or the 🎬 fallback for a thumbnail-less video) at the **top**, above Title, with a "✓ … added!" caption — so they see their media is coming along with the parsed story before submitting. New `#pvMedia` block + `renderVerifyMedia()`; shared visual helpers `mediaThumbSrc()` / `mediaLabel()` / `mediaVisualHTML()` drive both the input card and the verify card. If a Vimeo thumb arrives after the verify card is open, it refreshes there too.
- **Unchanged & re-verified:** auto-detect capture, either/or dimming, and "Change link"/remove reset all still work (reset also hides the verify preview); no console errors.

### 2026-06-27 (follow-up 2) — `vimeoThumb` server-side proxy for unlisted Vimeo thumbnails
Mark confirmed he's open to an Apps Script change when it improves functionality (see memory `feedback_open_to_apps_script`). This closes the one gap the JSONP path couldn't: **thumbnails for unlisted/private Vimeo videos** (the common case — people often upload personal clips as unlisted).
- **New Apps Script action `vimeoThumb`** (`apps-script-reference.gs`): public, no PIN; validates the URL is `vimeo.com`, calls **oEmbed server-side** (`vimeo.com/api/oembed.json?width=640&url=…` via `UrlFetchApp`, `muteHttpExceptions`), returns `{ ok, thumb }`. Server-side has no CORS limit and oEmbed returns thumbnails for unlisted links (the privacy hash rides along in the URL). Added to the router + `doGet` action list. No new Script Properties.
- **share.html now tries the proxy first, JSONP second.** `fetchVimeoThumb()` calls `vimeoThumb` via `postAction`; on success it applies the thumb, otherwise it falls back to the v2 JSONP path (public only), and if both come up empty the 🎬 icon fallback stays. New helper `applyVimeoThumb()`; the JSONP body moved to `jsonpVimeoThumb()`.
- **Graceful before redeploy:** until the owner redeploys, the live script returns "Unknown action" → the site silently uses JSONP (public thumbnails keep working). **Verified in the preview against the un-redeployed live script:** the proxy attempt fails over to JSONP and a public Vimeo thumbnail still loads; no console errors. `apps-script-reference.gs` passes `node --check`.
- **⭐ Owner action (optional but recommended):** paste `apps-script-reference.gs` and **redeploy a NEW web-app version** to switch on unlisted-Vimeo thumbnails. This rides along with the *other* already-pending redeploy (structural pass / Stage 2 / Story→Photo), so it costs no extra trip. See the dated `>>> CHANGED 2026-06-27` note at the top of the .gs. Nothing breaks if you skip it — you just won't get thumbnails for *unlisted* Vimeo links (public ones still work).

### 2026-06-27 (follow-up 3) — graceful video display: no third-party embed errors (index.html)
**Problem:** a submitted Vimeo video whose owner **disabled embedding** showed Vimeo's stark black "because of its privacy settings, this video cannot be played here" box in the story pop-out. We can't change other people's privacy settings, so we now detect it and show our own card instead.
- **Smart pre-check, then embed-or-button (index.html `openModal`).** Refactored the modal's media into `renderModalVideo(d)`:
  - **Vimeo** → shows the clean **button card by default**, and asks the server (`vimeoThumb`, now returning an `embeddable` flag) whether the video permits embedding; **only if `embeddable === true`** does it upgrade to the inline `player.vimeo.com/video/ID` iframe (private hash preserved). If embedding is disabled (or we can't tell) it stays the card — **Vimeo's error box is never rendered.**
  - **YouTube / Drive** → embed inline as before (these are ~always embeddable).
  - **Unrecognized video link** → button card.
- **The clean card** (`.video-card`): the row's photo as a poster (or a 🎬 clapboard if none) + a prominent on-brand **yellow "▶ Watch this video"** button that opens the source in a new tab. Warm, not an error. New helper `videoButtonCard()`; `videoInfo()` now also returns `kind` ('youtube'|'vimeo'|'drive').
- **Safety net:** an attempted iframe embed that hard-fails (`error` event) is swapped for the same button card. A per-open token guard (`currentVideoToken`, bumped on open and in `closeModal`) stops a slow async check from populating a closed/changed modal (no ghost audio).
- **Embeddability source.** `vimeoThumb` extended (apps-script-reference.gs): oEmbed returns an `<iframe>` in `html` only when embedding is allowed (→ `embeddable:true`); when disabled it 403s (→ `embeddable:false`). Backward-compatible — share.html's thumbnail use is unaffected (it reads `thumb`, which is unchanged).
- **Verified (preview, served from this repo):** YouTube/Drive embed inline; Vimeo shows the card then upgrades to inline only when embeddable (seeded the proxy answer to test both); private hash preserved (`?h=…`); not-embeddable stays the card; token guard holds; no console errors. Also confirmed against live Vimeo that an embeddable video's oEmbed returns 200 + `<iframe>` + thumbnail (so the server check is reliable).
- **⭐ Owner action — needs the SAME redeploy as follow-up 2** (the `embeddable` field rides in the same `vimeoThumb` change). The front-end is live and **degrades safely**: the live script still has the *old* `vimeoThumb` (no `embeddable` field), so **until you redeploy, ALL Vimeo videos show the button card** (no inline Vimeo, but also no error). **After the redeploy:** embeddable Vimeo plays inline; embedding-disabled Vimeo shows the card. YouTube/Drive are unaffected either way.

### 2026-06-27 (follow-up 4) — quiet the redundant bottom "Watch the video" button (index.html only)
Cosmetic; **no Apps Script, no redeploy.** The pop-out had a prominent teal "Watch the video →" button below the story text (from `modalBtnWrap`, fed by the row's `Button Label`/`Button URL`, which `addStoryFromPublic` sets to the video) — redundant with the inline player / clapboard card's own yellow button, and competing for attention.
- For a **video item** (`d.video` set), `modalBtnWrap` now renders a **subtle underlined blue text link "Watch the video"** (`.watch-link`, no arrow, no button fill) pointing at the source video (new tab) — a quiet last-resort, always present, that doesn't pull the eye from the inline player or the card's button.
- **Non-video items keep their custom row button** unchanged (the `.item-btn` path still applies when there's a `Button Label`/`Button URL` and no video) — e.g. a flyer linking to a PDF.
- Verified in the preview: video item → subtle blue link (clapboard `.vbc-btn` still present above); non-video custom button still renders as the teal `.item-btn`; no console errors.

## 2026-06-27 — rename to "High School Legacy" + domain change
Renamed the site from **High School History → High School Legacy** and the domain from **highschoolhistory.crutastic.com → highschoollegacy.crutastic.com**. **index.html + CNAME only; no Apps Script change, no redeploy.**
- **Domain (every hardcoded occurrence updated):**
  - `CNAME` → `highschoollegacy.crutastic.com`
  - `index.html` `og:url` meta → `https://highschoollegacy.crutastic.com`
  - HANDOFF.md doc references (the "Custom domain" line, the Site URL line, and the structural-pass checklist step 3) → new domain.
  - (Not found anywhere else — share.html / intake.html / history-intake.html / apps-script-reference.gs do **not** hardcode the domain.)
- **Displayed NAME "Cru High School History" → "Cru High School Legacy" (in `index.html`):**
  - `<title>` — **HTML default** (browser tab). ✅ updated.
  - `og:title` + `twitter:title` — **HTML defaults** (social share cards). ✅ updated.
  - Header `<h1 data-set="headerTitle">` — **HTML default, but Site-Settings-driven.** ✅ updated in HTML, **BUT if a saved `headerTitle` exists in the Site Settings tab it WINS** → owner must update it in the hidden editor (PIN 6060) or the header may still read "History".
  - Subtitle / nav labels were left alone — they contain no "History" (subtitle is "How God has used Cru in the lives of teenagers").
  - og/twitter **descriptions** ("…Cru's high school ministry…") left as-is — descriptive prose, not the site name.
- **Out of scope (intentionally NOT changed):** the `share.html` / `intake.html` / `history-intake.html` "Cru High School · 60th Anniversary" headings and the history-intake tool's "Ministry History Intake" name — those are different strings (60th-anniversary branding / a tool about ministry *history content*), not the site name. Flag if you want those renamed too.
- **⭐ Owner actions (both outside the repo — code is done):**
  1. **DNS / Pages:** create/point `highschoollegacy.crutastic.com` at GitHub Pages and set it as the custom domain (repo Settings → Pages). The CNAME file now serves that host; the old `highschoolhistory` domain stops working once DNS is moved.
  2. **Hidden editor (PIN 6060):** if `headerTitle` was ever saved in Site Settings, change it to **"Cru High School Legacy"** (same for any other saved name text). HTML-default spots (title/og/twitter) need nothing — they're already updated.
- **No redeploy:** the Apps Script is untouched.

## 2026-06-27 — "See more from the decade" link in the card pop-out (index.html only)
**Front-end only; no Apps Script, no redeploy.** Bridges the Timeline and the fuller Stories collection: at the bottom of a card pop-out, a subtle link jumps to the Stories view pre-filtered to that card's decade.
- **The link.** In `openModal`, a new `#modalDecadeLink` (last element in `.m-body`) renders `<a class="watch-link" … data-decade-jump="1960s">See more from the 1960s →</a>` when the item has a year — same restrained underlined-blue treatment as the "Watch the video" link, with the arrow. **Omitted when the card has no year** (no decade to point to). The decade text comes from the existing `decadeOf(year)` (1967 → "1960s").
- **Navigation reuses the existing decade filter.** New `goToDecade(decade)` sets `filters = {decade, …}`, activates the matching button in the Stories decade bar (the bar is built by `buildFilterOptions`, so the active class is toggled here just like the bar's own click handler and `goToYear` do), calls `renderGallery()`, and `switchView("gallery")`. Wired in the global click delegation next to the year/location/person handlers: `data-decade-jump` → `preventDefault` + `closeModal()` + `goToDecade(...)`. **In-page state only — no URL hash, no localStorage** (fits the existing SPA pattern).
- **Appears on every card pop-out with a year** (the modal is shared by Timeline and Stories cards) — so all timeline cards get it; gallery cards do too (harmless, occasionally redundant). Say the word if you want it Timeline-only.
- **Verified (preview):** a 1967 card shows "See more from the 1960s →"; clicking lands on the Stories view with the **1960s decade button active** (only that one) and **only 1960s cards** showing, modal closed, Stories nav highlighted; a no-year item omits the link; no console errors. Existing timeline / decade filter / gallery / other views untouched.

## Earlier next task (now done) — build `share.html`
Built as Stage 1 above. Stage 2 (auto-rename) remains.
